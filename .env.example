NOVEL_PROJECT_PATH=/absolute/path/to/your/webnovel-project
GENERATION_API_KEY=
# Optional. If left empty, Chat uses the built-in local mode and remains directly usable.
LOG_LEVEL=INFO
CORS_ORIGIN=http://localhost:8765
# Optional: enable Basic Auth in Docker/Compose deployments only.
# WEBNOVEL_DASHBOARD_BASIC_AUTH=writer:change-me

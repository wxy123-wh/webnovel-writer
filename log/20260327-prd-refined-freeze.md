# 2026-03-27 prd refined freeze

## 本次关键改动

1. 新增 `docs/prd-refined.md`，将 `docs/prd.md` 的高层目标冻结为可开发规格。
2. 固化用户确认项：
   - 前端四页纯展示，写入口 UI 隐藏。
   - 写接口采用物理删除策略。
   - 新增统一 CLI 命令（`webnovel codex`）。
   - 索引对象为章节正文/总纲/细纲，采用文件变更自动触发的增量索引。
   - 快速定位主键为章节号与场景标签。
   - Skill 采用人工选择 profile，仅会话加载，不进入 Codex 全局。
3. 新增 RAG 13档验收指标（连通性+正确性+性能）并定义阈值：
   - 正确性：Hit@5、MRR@10、章节约束正确率。
   - 性能：检索 p95/p99、增量索引 p95、变更到可检索可见时间 p95。
4. 文档内同步给出三类交付：
   - 可执行改造清单
   - 接口变更清单
   - M1/M2/M3 里程碑

## 说明

- 本文档是当前需求冻结稿，可作为后续实施与拆任务的唯一输入。
- 后续若改动系统边界或命令入口，需重新评估并同步 `.nexus-map/`。
- 基线口径已确认：按当前工作区未提交改动继续推进，不要求回退。

# Webnovel Writer - 核心生产依赖
# Python >= 3.10 required

# 异步 HTTP 客户端（API 调用）
aiohttp>=3.8.0

# 文件锁（状态文件并发控制）
filelock>=3.0.0

# Schema 校验
pydantic>=2.0.0

# 文件监听（可选，用于 codex index watch 命令）
watchdog>=5.0.0
